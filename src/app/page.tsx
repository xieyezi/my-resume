import { ResumeTemplate } from "@/components/ResumeTemplate"; // Fixed the import for ResumeTemplate component

export default function Home() {
  return (
    <main className="min-h-screen">
      <ResumeTemplate />
    </main>
  );
}
